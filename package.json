{
  "name": "parley",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Parley — a local CLI that exports public LLM share conversations (ChatGPT, DeepSeek, Gemini, Kimi, Claude, Grok) to Markdown.",
  "license": "MIT",
  "bin": {
    "parley": "bin/parley.mjs"
  },
  "scripts": {
    "check": "sh -c 'for f in bin/*.mjs src/*.mjs src/lib/*.mjs src/providers/*.mjs test/*.mjs; do node --check \"$f\" || exit 1; done'",
    "lint": "eslint .",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  }
}
